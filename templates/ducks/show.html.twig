{% extends 'base.html.twig' %}

{% block title %}Quack{% endblock %}

{% block body %}
    <h1>Quack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Firstname</th>
                <td>{{ duck.firstname }}</td>
            </tr>
            <tr>
                <th>Duckname</th>
                <td>{{ duck.duckname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ duck.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ducks_index') }}">back to list</a>

    <a href="{{ path('duck_edit', {'id': duck.id}) }}">edit</a>

    {{ include('ducks/_delete_form.html.twig') }}
{% endblock %}
